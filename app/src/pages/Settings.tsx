import { PlaceholderPage } from "./PlaceholderPage";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      phase="C6"
      description={
        <>
          Owner-only. Movies catalog (with CSV upload), Price Cards per screen,
          Screens &amp; Tax config, Authorized Users.
        </>
      }
    />
  );
}
