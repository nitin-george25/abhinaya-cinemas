import { PlaceholderPage } from "./PlaceholderPage";

export default function ActivityPage() {
  return (
    <PlaceholderPage
      title="Activity log"
      phase="C6"
      description={
        <>
          Recent changes across the cloud — who touched what, when, on which
          screen. Filter by user / change type / date.
        </>
      }
    />
  );
}
