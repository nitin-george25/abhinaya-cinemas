{
  "version": 1,
  "description": "Subdomain split (2026-06-04): the landing project no longer runs any Pages Functions. /api/* used to route to /functions/api/contact.ts (now tombstoned — contact form moved to a Supabase Edge Function). Empty include leaves all paths to strict static serving with _redirects rules.",
  "include": [],
  "exclude": []
}
